-- パスワードでのログインを撤去した（#149）。ログインはメールに届くコードだけになり、パスワードのハッシュは使われない。
-- 使わないパスワードの情報を持ち続けると漏えい時の被害が増えるため、列ごと消す（オーナー確定 #144 確定事項14）。
-- 元には戻せないが、パスワードを持っていたのは開発用の初期ユーザーだけで、失われる実データは無い。
ALTER TABLE public.organization_users DROP COLUMN password_hash;
